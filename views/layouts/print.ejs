<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Print View' %></title>
    <style>
        /* Print-specific styling */
        @page {
            size: letter portrait;
            margin: 0.5in;
        }

        * {
            box-sizing: border-box;
            font-family: Arial, Helvetica, sans-serif;
        }

        body {
            margin: 0;
            padding: 0;
            font-size: 12pt;
            line-height: 1.5;
            background: #fff;
            color: #000;
        }

        .print-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 2px solid #000;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }

        .logo-container {
            width: 150px;
        }

        .logo-container img {
            max-width: 100%;
            height: auto;
        }

        .title-container {
            text-align: center;
            flex-grow: 1;
        }

        .print-title {
            font-size: 24pt;
            font-weight: bold;
            margin: 0;
        }

        .print-subtitle {
            font-size: 14pt;
            margin: 5px 0 0 0;
        }

        .section {
            margin-bottom: 20px;
        }

        .section-title {
            font-size: 14pt;
            font-weight: bold;
            border-bottom: 1px solid #000;
            padding-bottom: 5px;
            margin-bottom: 10px;
        }

        .info-group {
            display: flex;
            flex-wrap: wrap;
            margin-bottom: 15px;
        }

        .info-item {
            width: 50%;
            margin-bottom: 5px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        table th, table td {
            border: 1px solid #000;
            padding: 8px;
            text-align: left;
        }

        table th {
            background-color: #f2f2f2;
            font-weight: bold;
        }

        .summary-box {
            border: 1px solid #000;
            padding: 10px;
            background-color: #f2f2f2;
            margin-bottom: 20px;
        }

        .signature-section {
            margin-top: 30px;
            border-top: 1px dashed #000;
            padding-top: 15px;
        }

        .signature-box {
            border-bottom: 1px solid #000;
            height: 100px;
            margin-bottom: 5px;
        }

        .signature-label {
            font-weight: bold;
        }

        .warning {
            background-color: #ffdddd;
            border: 1px solid #f44336;
            padding: 10px;
            margin-bottom: 20px;
        }

        .notice {
            background-color: #e7f3fe;
            border: 1px solid #2196F3;
            padding: 10px;
            margin-bottom: 20px;
        }

        .footer {
            margin-top: 30px;
            text-align: center;
            font-size: 10pt;
            color: #666;
            border-top: 1px solid #ccc;
            padding-top: 10px;
        }

        @media print {
            body {
                margin: 0;
                padding: 0;
                background: #fff;
            }

            .no-print {
                display: none !important;
            }

            a {
                text-decoration: none;
                color: #000;
            }
        }
    </style>
</head>
<body>
<%- body %>

<div class="footer">
    Printed by <%= user ? user.username : 'Guest' %> on <%= new Date().toLocaleString() %>
</div>

<script>
    // Auto-print when page loads
    window.addEventListener('load', function() {
        setTimeout(function() {
            window.print();
        }, 500);
    });
</script>
</body>
</html>